#!/usr/bin/env bash
# LiSin v1 launcher: system PySide6 + Kirigami, no pip PySide6 from the user
# site (which would shadow the system Kirigami).
export PYTHONNOUSERSITE=1
export QT_QUICK_CONTROLS_STYLE=org.kde.desktop
exec python3 "$(dirname "$(readlink -f "$0")")/lisin_app.py" "$@"
