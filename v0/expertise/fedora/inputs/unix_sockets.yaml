name: unix_sockets
id: LS-I-36
type: input
version: 1.0.0
title: Unix sockets
# The local IPC surface: listening unix sockets + the owner (if it is ours).
command: ss -xlpH 2>/dev/null
interval: 60
enabled: true
