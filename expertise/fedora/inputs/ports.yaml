name: ports
id: LS-I-3
type: input
version: 3.0.0
title: Network sockets
# ALL sockets (listen + established) with a single ss - ports and connections are
# the same thing. + a firewall section for the exposure of the listeners.
command: |
  ss -H -tunapn
  echo ---FWPORTS---
  for z in $(firewall-cmd --get-active-zones 2>/dev/null | grep -v '^ '); do
    echo "zone=$z"
    firewall-cmd --zone="$z" --list-ports 2>/dev/null
    for s in $(firewall-cmd --zone="$z" --list-services 2>/dev/null); do
      firewall-cmd --info-service="$s" 2>/dev/null | sed -n 's/.*ports: //p'
    done
  done
  true
interval: 30
enabled: true
