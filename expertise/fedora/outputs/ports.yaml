name: db_ports
id: LS-O-ports
type: statedb
version: 2.0.0
title: Network sockets
table: ports
key:
- proto
- local
- remote
icon: network-connect
# ПЕРЕХОДЫ ЗДЕСЬ НЕ ОТСЛЕЖИВАЕМ. Проверено на живых данных: сокеты текучи —
# за один прогон 16 появилось, 28 исчезло, и лента заполнялась бы сводками
# «массовое изменение» каждые 20 секунд. Открытие и закрытие соединений уже
# даёт источник netmon, там это событие по существу, а не разница снимков.
